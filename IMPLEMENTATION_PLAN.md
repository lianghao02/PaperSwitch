# 實作計畫

## 目標與驗收條件

維持 PaperSwitch 作為離線 PDF 排列器：中央只呈現頁面縮圖、頁碼、選取狀態與必要檔名，不引入照片欄位編輯器。

- [ ] 縮圖卡片的檔名固定單行省略，頁碼與選取框保持可辨識。
- [ ] 顯示「已選取 N 頁」，並提供 Shift 區間選取、框選與快捷鍵提示。
- [ ] 方向鍵焦點、Shift 擴選、Ctrl+左右重排、Delete 移除的規則一致且可發現。
- [ ] Undo/Redo 可復原畫布編輯，不影響來源檔案。
- [ ] 大量頁面以可視範圍優先方式排程縮圖，維持背景載入。
- [ ] 繼續保留右上縮圖尺寸滑桿、px 數值與 Ctrl+滾輪快速縮放。

## 不做範圍

- 不複製 `04_Photo-Report-Generator` 的右側照片資料欄位或編輯 UI。
- 不改變 PDF 向量無損匯出、Office COM 與 IGEF 安全邊界。
- 不以卡片塞入檔案屬性、註解或照片編輯欄位。

## 現況與限制

- 現有畫布為居中 WrapPanel 縮圖卡片，已具頁碼、旋轉標記、選取框、拖曳與框選。
- Shift+點擊連續選取、Ctrl+點擊、框選、Ctrl+方向鍵重排與 Ctrl+滾輪已存在。
- 縮圖目前於匯入後以 `Task.WhenAll` 背景排程，快取已有 180 筆 LRU 上限與同 key 併發去重；尚未依可視範圍排序。

## 已確認決策

- 中央畫布採「縮圖排列器」而非「資料編輯器」。
- 檔名僅作來源辨識：單行、尾端省略；不占用縮圖閱讀區。
- 操作提示採常駐精簡提示加 ToolTip，不增加右側屬性面板。
- 大量頁面先保留既有 WrapPanel 版面，優先改善縮圖排程；只有實測仍不足才評估虛擬化面板。

## 工作清單

- [x] 精簡卡片底部資訊與檔名單行截斷｜縮放 90～480px 不換行、不遮蔽頁碼。
- [x] 增加選取數量與快捷鍵提示｜空白、單選、多選時皆可理解操作。
- [x] 補齊方向鍵焦點與 Shift 擴選規則｜建置通過；待實機鍵盤互動驗證。
- [x] 建立 Undo/Redo 與 A4 空白頁基礎｜後續補齊單元測試與介面驗證。
- [x] 匯入批次採有限併發背景縮圖排程｜每批最多四張同時渲染；可視範圍優先列為下一階段優化。
- [ ] 發行前更新 README、CHANGELOG、測試與 GitHub 同步｜交付前檢查。

## 風險與因應

- WPF WrapPanel 不支援原生虛擬化：先做延遲縮圖與取消排程，避免不必要的控制項重構。
- 大量卡片的焦點導航易與重排語意混淆：純方向鍵只移焦點，Ctrl+方向鍵才改變排序，Shift+方向鍵只擴選。

## 驗證紀錄

- 2026-08-30：規劃前檢視確認既有縮圖畫布、Shift 點選、框選、Ctrl+方向鍵與 Ctrl+滾輪功能。
- 2026-08-30：完成卡片檔名單行化、選取／快捷鍵提示、方向鍵焦點與 Shift 擴選、有限併發縮圖載入；`dotnet build -c Release --no-restore` 通過（0 警告、0 錯誤）。

## 剩餘問題

- 需以 200～500 頁實際 PDF 量測延遲縮圖排程後的初始載入與捲動回應。
