# spec.md — 需求規格與功能邊界

## 1. 需求範疇
1. **Word 轉 PDF**：支援 `.doc` 與 `.docx` 格式轉換，排版需 100% 保持一致。
2. **Excel 轉 PDF**：支援 `.xls` 與 `.xlsx` 格式轉換，預設自動開啟寬度縮放單頁適應。
3. **圖片轉 PDF**：支援 `.jpg`, `.png`, `.bmp`, `.webp` 轉檔與合成。
4. **Web 視窗介面**：自動開啟本機瀏覽器，提供視覺化拖拽上傳。

## 2. 非功能性需求
- **轉換速度**：單檔轉換時間在 3 秒內完成。
- **無狀態與安全性**：檔案上傳至 `uploads/` 並產出至 `converted/`，不對外曝光機密資訊。
