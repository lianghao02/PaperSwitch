# DESIGN.md — UI/UX 設計規範與 Design System

## 1. 視覺主題與莫蘭迪色系 (Morandi Color System)
本工具採用 **沉穩優雅的莫蘭迪色系 (Morandi Palette)** 搭配深色玻璃擬物 (Dark Glassmorphism) 風格，低飽和灰調視覺溫潤舒適，徹底消除刺眼光害：

```
+-------------------------------------------------------------------------------+
|                              HEADER (固定標題區)                              |
+-----------------------+-----------------------------+-------------------------+
|  📥 1. 檔案拖曳區     |  📋 2. 檔案佇列與進度       |  ⚙️ 3. 轉換功能設定     |
|  • 拖曳/選擇檔案      |  • 總體真進度條             |  • 四大工整模式選擇     |
|  • 格式標籤導引       |  • 檔案佇列卡片             |  • 合併檔名輸入         |
|  • 等高彈性伸展       |  • 即時動態日誌終端         |  • 🚀 開始轉換按鈕      |
|                       |                             |  • 維護工具區           |
+-----------------------+-----------------------------+-------------------------+
```

## 2. 版面佈局規範 (Layout Specification)
- **總體寬度**：佔視窗寬度約 **82vw（約 80%）**，最大寬度 `1600px`，居中排版，兩側保留黃金比例留白。
- **流水線操作視線**：由左至右 **① 拖曳加入 ➔ ② 檢視佇列/進度/日誌 ➔ ③ 設定模式與啟動轉換**，完全符合人性化心智模型。
- **三欄等高對齊**：三欄面板高度自適應視窗（`height: calc(100vh - 120px)`），消除外層滾動條。

## 3. 字型與層級規範 (Typography Hierarchy)
- **H1 頁首標題 (`header h1`)**: `2.1rem` / `800 ExtraBold` / 亮白高光漸層，字距 `-0.03em`。
- **H2 區塊標題 (`.panel-title`)**: `1.22rem` / `800 ExtraBold` / 純白 `#ffffff`，搭配 `2px` 霧天藍底線強化視覺錨點。
- **H3 選項/卡片標題 (`.radio-title`, `.drop-text`)**: `1.02rem ~ 1.16rem` / `800` / `#ffffff`。
- **內文/檔案名稱 (`.file-name`, `.file-meta`)**: `0.92rem` / `700 Bold` / 醒目純白。
- **輔助說明 (`.radio-desc`, `.drop-hint`)**: `0.82rem ~ 0.92rem` / `500 Medium` / 高對比淺藍灰 `#94a3b8`。
- **日誌與終端 (`.log-console`)**: `0.78rem` / `Consolas` 等寬字型 / 行高 `1.45`。

## 4. 色彩代碼 (Design Tokens)
- **背景主色 (`--bg-color`)**: `#161c28` (低飽和莫蘭迪霧深藍灰)
- **卡片面板背景 (`--card-bg`)**: `rgba(26, 34, 48, 0.88)` (莫蘭迪霧板岩灰，搭配 16px Blur)
- **面板內嵌底色 (`--panel-bg`)**: `rgba(18, 24, 35, 0.65)`
- **強調主色 (`--accent-color`)**: `#6ba4c8` (莫蘭迪霧天藍)
- **強調懸停色 (`--accent-hover`)**: `#568eb2`
- **成功色 (`--success-color`)**: `#7ea88f` (莫蘭迪鼠尾草綠)
- **警告色 (`--warning-color`)**: `#d1a368` (莫蘭迪灰杏黃)
- **錯誤色 (`--error-color`)**: `#d47a7a` (莫蘭迪豆沙灰紅)
- **邊框色彩 (`--border-color`)**: `rgba(255, 255, 255, 0.11)`
- **文字 H1 (`--text-h1`)**: `#ffffff` (100% 純白)
- **文字 H2 (`--text-h2`)**: `#f1f5f9` (95% 亮白)
- **文字 H3 (`--text-h3`)**: `#e2e8f0` (88% 淺白)
- **文字內文 (`--text-body`)**: `#cbd5e1` (80% 淺灰)
- **副文字 (`--text-sub`)**: `#94a3b8` (60% 藍灰)

## 5. 互動與動效反饋
- **呼吸發光 (Pulse Glow)**：轉檔中標籤自帶柔和呼吸光芒 (`animation: pulseGlow 1.4s infinite`)。
- **真進度即時推播**：進度條與狀態文字隨每檔轉換進度精確連動。
